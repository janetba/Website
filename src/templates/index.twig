{% include 'header.twig' %}

<p><a href="{{ app.request.baseUrl ~ '/get' }}" class="btn"><b class="icon-plus-sign"></b> Get a Photo</a></p>


 <div class="control-group">
    <label class="control-label" for="photoFile">Photo to Get</label>
    <div class="controls">
      <label>FileName: </label> 
	  <input type="text" name="photoIndex" id="photoIndex">
	  <input type="button" name="photoIndexSubmit" id="photoIndexSubmit">
    </div>
  </div>

<!-- div class="row-fluid">
    {%  for image in images %}
        <div class="uploaded-image">
            <img src="{{ image.url }}" class="img-polaroid">
            <p class="caption">{{ image.caption }}</p>
        </div>
    {%  endfor %}
</div -->

{% include 'footer.twig' %}